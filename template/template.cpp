#include <bits/stdc++.h>
using namespace std;

void run_case() {$1}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int tc;
  cin >> tc;
  for (int i = 1; i <= tc; ++i) {
    run_case();
  }
  return 0;
}
