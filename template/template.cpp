#include <bits/stdc++.h>
using namespace std;

void run_case() {$1}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int tt;
  cin >> tt;
  for (int i = 0; i < tt; ++i) {
    run_case();
  }
  return 0;
}
